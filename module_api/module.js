module.exports = {
  renderStatus: ($domNode) => {},
  renderWindow: (params) => {},
  renderFeed: (addFeedItem: function) => {},
  onVoiceCommand: (command, answerCB) => {[answerCB(answer: string)]; return (doesAnswerWithVoice: boolean) }
}
